enable_testing()

add_executable(
    engine_tests
    StreamConsumerTest.cpp
)

target_link_libraries(
    engine_tests
    matching_core
    GTest::gtest_main
)

include(GoogleTest)
gtest_discover_tests(engine_tests)
